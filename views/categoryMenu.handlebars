<!-- corner logo -->
<div id="landing-corner-logo">
  <div class="round corner-circle"></div>
  <h5 class="corner-text">512 BINGO</h5>
</div>

<!-- mobile category menu -->
<section id="mobile-category" class="hide-on-med-and-up">
  <h3 class="center red">{{category}}</h3>
  <ul class="menu-list">
    {{#each card}}
    <li>{{card_name}}</li>
    {{/each}}
  </ul>
</section>

<!-- tablet+ category menu -->
<section id="desk-category" class="hide-on-small-only center">
  <p class="sign-out">
    sign out
  </p>
  <h3 class="red">{{category}}</h3>
  <div class="">
    {{#each card}}
    <div class="category-box">
      <div class="">
        <h4>{{card_name}}</h4>
      </div>
    </div>
    {{/each}}
  </div>
</section>
